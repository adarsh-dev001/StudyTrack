# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Operating System Files
.DS_Store
Thumbs.db
.localized
.AppleDouble
.LSOverride

# Node.js Dependencies and Build Artifacts
/node_modules
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
/.pnpm-debug.log*

# Yarn PnP (Plug'n'Play) and Cache
/.pnp/
.pnp.cjs
.pnp.mjs
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js Build Outputs
/.next/
/out/
/build/ # Common alternative build output

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase Specific Files and Build Artifacts
# This is CRUCIAL for ignoring your Firebase functions' build outputs and cache!
/.firebase/
firebase-debug.log
firestore-debug.log
.firebaserc # Generally this is committed, but if sensitive/local-only, keep ignored.

# Genkit related files (if applicable)
.genkit/
.genkit/*

# Vercel Deployment Files
.vercel/

# TypeScript Build Artifacts
*.tsbuildinfo
next-env.d.ts

# Editor / IDE specific files (Add as needed)
.vscode/        # VSCode settings (if not shared)
.idea/          # IntelliJ/WebStorm
*.sublime-project
*.sublime-workspace
.project
.classpath
.settings/
.cache/

# Misc
*.pem # Private key files
StudyTrack_backup/.firebase/studyplanner-4abf5/functions/.next/BUILD_ID
# Ignore backup builds (like .next inside backups)
/StudyTrack_backup/
/StudyTrack_backup/**/.next/
